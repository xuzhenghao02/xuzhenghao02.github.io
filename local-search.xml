<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Java线程池实现原理</title>
    <link href="/%5Bobject%20Object%5D/2023/07/Java%E5%B9%B6%E5%8F%91-Java%E7%BA%BF%E7%A8%8B%E6%B1%A0%E5%AE%9E%E7%8E%B0%E5%8E%9F%E7%90%86/"/>
    <url>/%5Bobject%20Object%5D/2023/07/Java%E5%B9%B6%E5%8F%91-Java%E7%BA%BF%E7%A8%8B%E6%B1%A0%E5%AE%9E%E7%8E%B0%E5%8E%9F%E7%90%86/</url>
    
    <content type="html"><![CDATA[<h1 id="Java线程池实现原理"><a href="#Java线程池实现原理" class="headerlink" title="Java线程池实现原理"></a>Java线程池实现原理</h1><h2 id="线程池介绍"><a href="#线程池介绍" class="headerlink" title="线程池介绍"></a>线程池介绍</h2><h3 id="线程池是什么"><a href="#线程池是什么" class="headerlink" title="线程池是什么"></a>线程池是什么</h3><p>线程池（Thread Pool）是一种基于池化思想管理线程的工具，经常出现在多线程服务器中，如MySQL。</p><p>线程过多会带来额外的开销，其中包括创建销毁线程的开销、调度线程的开销等等，同时也降低了计算机的整体性能。线程池维护多个线程，等待监督管理者分配可并发执行的任务。这种做法，一方面避免了处理任务时创建销毁线程开销的代价，另一方面避免了线程数量膨胀导致的过分调度问题，保证了对内核的充分利用。</p><p>而本文描述线程池是JDK中提供的ThreadPoolExecutor类。</p><p>当然，使用线程池可以带来一系列好处：</p><ul><li>降低资源消耗：通过池化技术重复利用已创建的线程，降低线程创建和销毁造成的损耗。</li><li>提高响应速度：任务到达时，无需等待线程创建即可立即执行。</li><li>提高线程的可管理性：线程是稀缺资源，如果无限制创建，不仅会消耗系统资源，还会因为线程的不合理分布导致资源调度失衡，降低系统的稳定性。使用线程池可以进行统一的分配、调优和监控。</li><li>提供更多更强大的功能：线程池具备可拓展性，允许开发人员向其中增加更多的功能。比如延时定时线程池ScheduledThreadPoolExecutor，就允许任务延期执行或定期执行。</li></ul><h3 id="线程池解决的问题是什么"><a href="#线程池解决的问题是什么" class="headerlink" title="线程池解决的问题是什么"></a>线程池解决的问题是什么</h3><p>线程池解决的核心问题就是资源管理问题。在并发环境下，系统不能够确定在任意时刻中，有多少任务需要执行，有多少资源需要投入。这种不确定性将带来以下若干问题：</p><ol><li>频繁申请&#x2F;销毁资源和调度资源，将带来额外的消耗，可能会非常巨大。</li><li>对资源无限申请缺少抑制手段，易引发系统资源耗尽的风险。</li><li>系统无法合理管理内部的资源分布，会降低系统的稳定性。</li><li>为解决资源分配这个问题，线程池采用了“池化”（Pooling）思想。池化，顾名思义，是为了最大化收益并最小化风险，而将资源统一在一起管理的一种思想。</li></ol><p>在计算机领域中的表现为：统一管理IT资源，包括服务器、存储、和网络资源等等。通过共享资源，使用户在低投入中获益。除去线程池，还有其他比较典型的几种使用策略包括：</p><ul><li>内存池(Memory Pooling)：预先申请内存，提升申请内存速度，减少内存碎片。</li><li>连接池(Connection Pooling)：预先申请数据库连接，提升申请连接的速度，降低系统的开销。</li><li>实例池(Object Pooling)：循环使用对象，减少资源在初始化和释放时的昂贵损耗。</li></ul><h2 id="线程池设计"><a href="#线程池设计" class="headerlink" title="线程池设计"></a>线程池设计</h2><h3 id="总体设计"><a href="#总体设计" class="headerlink" title="总体设计"></a>总体设计</h3><p><img src="/../images/dailyrecord/%E7%BA%BF%E7%A8%8B%E6%B1%A0%E7%B1%BB%E8%AE%BE%E8%AE%A1.png" alt="img"><br>线程池中最顶级的接口是Executor，只包含execute接口，顶层接口Executor提供了一种思想：将任务提交和任务执行进行解耦。用户无需关注如何创建线程，如何调度线程来执行任务，用户只需提供Runnable对象，将任务的运行逻辑提交到执行器(Executor)中，由Executor框架完成线程的调配和任务的执行部分。</p><p>ExecutorService接口增加了一些能力：<br>（1）扩充执行任务的能力，例如可以为一个或一批异步任务生成Future的submit方法，获取第一个方法的返回值的invokeAny方法，获取所有线程返回值的invokeAll方法<br>（2）提供了管控线程池的方法，比如停止线程池运行的shutdown方法</p><p>AbstractExecutorService则是上层的抽象类，将执行任务的流程串联了起来，保证下层的实现只需关注一个执行任务的方法即可。实现了invokeAny、invokeAll、submit等ExecutorService接口的方法，还创建了newTaskFor这个返回RunnableFuture的方法（RunnableFuture接口同时继承了Runnable和Future这两个接口）</p><p>最下层的实现类ThreadPoolExecutor实现最复杂的运行部分，ThreadPoolExecutor将会一方面维护自身的生命周期，另一方面同时管理线程和任务，使两者良好的结合从而执行并行任务。</p><p>ThreadPoolExecutor是如何运行，如何同时维护线程和执行任务的呢？其运行机制如下图所示：<br><img src="/../images/dailyrecord/ThreadPoolExecutor%E8%BF%90%E8%A1%8C%E6%B5%81%E7%A8%8B.png" alt="img"><br>线程池在内部实际上构建了一个生产者消费者模型，将线程和任务两者解耦，并不直接关联，从而良好的缓冲任务，复用线程。线程池的运行主要分成两部分：任务管理、线程管理。<strong>任务管理部分充当生产者的角色</strong>，当任务提交后，线程池会判断该任务后续的流转：（1）直接申请线程执行该任务；（2）缓冲到队列中等待线程执行；（3）拒绝该任务。<strong>线程管理部分是消费者</strong>，它们被统一维护在线程池内，根据任务请求进行线程的分配，当线程执行完任务后则会继续获取新的任务去执行，最终当线程获取不到任务的时候，线程就会被回收。</p><p>下面分成三个部分去详细讲解线程池运行机制：</p><ol><li>线程池如何维护自身状态。</li><li>线程池如何管理任务。</li><li>线程池如何管理线程。</li></ol><h3 id="生命周期管理"><a href="#生命周期管理" class="headerlink" title="生命周期管理"></a>生命周期管理</h3><p>线程池运行的状态，并不是用户显式设置的，而是伴随着线程池的运行，由内部来维护。线程池内部使用一个变量维护两个值：运行状态(runState)和线程数量 (workerCount)。在具体实现中，线程池将运行状态(runState)、线程数量 (workerCount)两个关键参数的维护放在了一起，如下代码所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">AtomicInteger</span> <span class="hljs-variable">ctl</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AtomicInteger</span>(ctlOf(RUNNING, <span class="hljs-number">0</span>));<br></code></pre></td></tr></table></figure><p>ctl这个AtomicInteger类型，是对<strong>线程池的运行状态</strong>和<strong>线程池中有效线程的数量</strong>进行控制的一个字段。它同时包含两部分的信息：线程池的运行状态 (runState) 和线程池内有效线程的数量 (workerCount)，高3位保存runState，低29位保存workerCount，两个变量之间互不干扰。用一个变量去存储两个值，可避免在做相关决策时，出现不一致的情况，不必为了维护两者的一致，而占用锁资源。通过阅读线程池源代码也可以发现，经常出现要同时判断线程池运行状态和线程数量的情况。线程池也提供了若干方法去供用户获得线程池当前的运行状态、线程个数。这里都使用的是位运算的方式，相比于基本运算，速度也会快很多。</p><p>关于内部封装的获取生命周期状态、获取线程池线程数量的计算方法如以下代码所示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">runStateOf</span><span class="hljs-params">(<span class="hljs-type">int</span> c)</span>     &#123; <span class="hljs-keyword">return</span> c &amp; ~CAPACITY; &#125; <span class="hljs-comment">//计算当前运行状态</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">workerCountOf</span><span class="hljs-params">(<span class="hljs-type">int</span> c)</span>  &#123; <span class="hljs-keyword">return</span> c &amp; CAPACITY; &#125;  <span class="hljs-comment">//计算当前线程数量</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">ctlOf</span><span class="hljs-params">(<span class="hljs-type">int</span> rs, <span class="hljs-type">int</span> wc)</span> &#123; <span class="hljs-keyword">return</span> rs | wc; &#125;   <span class="hljs-comment">//通过状态和线程数生成ctl</span><br></code></pre></td></tr></table></figure><p>ThreadPoolExecutor的运行状态有5种，分别为：<br><img src="/../images/dailyrecord/ThreadPoolExecutor%E8%BF%90%E8%A1%8C%E6%B5%81%E7%A8%8B.png" alt="img"><br>其生命周期转换如下入所示：<br><img src="/../images/dailyrecord/%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F%E5%9B%BE.png" alt="img"></p><h3 id="任务执行机制"><a href="#任务执行机制" class="headerlink" title="任务执行机制"></a>任务执行机制</h3><h4 id="任务调度"><a href="#任务调度" class="headerlink" title="任务调度"></a>任务调度</h4><p>任务调度是线程池的主要入口，当用户提交了一个任务，接下来这个任务将如何执行都是由这个阶段决定的。了解这部分就相当于了解了线程池的核心运行机制。</p><p>首先，所有任务的调度都是由execute方法完成的，这部分完成的工作是：检查现在线程池的运行状态、运行线程数、运行策略，决定接下来执行的流程，是直接申请线程执行，或是缓冲到队列中执行，亦或是直接拒绝该任务。其执行过程如下：</p><ol><li>首先检测线程池运行状态，如果不是RUNNING，则直接拒绝，线程池要保证在RUNNING的状态下执行任务。</li><li>如果workerCount &lt; corePoolSize，则创建并启动一个线程来执行新提交的任务。</li><li>如果workerCount &gt;&#x3D; corePoolSize，且线程池内的阻塞队列未满，则将任务添加到该阻塞队列中。</li><li>如果workerCount &gt;&#x3D; corePoolSize &amp;&amp; workerCount &lt; maximumPoolSize，且线程池内的阻塞队列已满，则创建并启动一个线程来执行新提交的任务。</li><li>如果workerCount &gt;&#x3D; maximumPoolSize，并且线程池内的阻塞队列已满, 则根据拒绝策略来处理该任务, 默认的处理方式是直接抛异常。</li></ol><p>其执行流程如下图所示：<br><img src="/../images/dailyrecord/%E4%BB%BB%E5%8A%A1%E8%B0%83%E5%BA%A6%E6%B5%81%E7%A8%8B.png" alt="img"></p><h5 id="任务缓冲"><a href="#任务缓冲" class="headerlink" title="任务缓冲"></a>任务缓冲</h5><p>任务缓冲模块是线程池能够管理任务的核心部分。线程池的本质是对任务和线程的管理，而做到这一点最关键的思想就是将任务和线程两者解耦，不让两者直接关联，才可以做后续的分配工作。线程池中是以生产者消费者模式，通过一个阻塞队列来实现的。阻塞队列缓存任务，工作线程从阻塞队列中获取任务。</p><p>阻塞队列(BlockingQueue)是一个支持两个附加操作的队列。这两个附加的操作是：在队列为空时，获取元素的线程会等待队列变为非空。当队列满时，存储元素的线程会等待队列可用。阻塞队列常用于生产者和消费者的场景，生产者是往队列里添加元素的线程，消费者是从队列里拿元素的线程。阻塞队列就是生产者存放元素的容器，而消费者也只从容器里拿元素。</p><p>下图中展示了线程1往阻塞队列中添加元素，而线程2从阻塞队列中移除元素：<br><img src="/../images/dailyrecord/%E9%98%BB%E5%A1%9E%E9%98%9F%E5%88%97.png" alt="img"><br>使用不同的队列可以实现不一样的任务存取策略。在这里，我们可以再介绍下阻塞队列的成员：<br><img src="/../images/dailyrecord/%E4%B8%8D%E5%90%8C%E7%9A%84%E7%BA%BF%E7%A8%8B%E6%B1%A0%E9%98%9F%E5%88%97.png" alt="img"></p><h4 id="任务申请"><a href="#任务申请" class="headerlink" title="任务申请"></a>任务申请</h4><p>由上文的任务分配部分可知，任务的执行有两种可能：一种是任务直接由新创建的线程执行。另一种是线程从任务队列中获取任务然后执行，执行完任务的空闲线程会再次去从队列中申请任务再去执行。第一种情况仅出现在线程初始创建的时候，第二种是线程获取任务绝大多数的情况。</p><p>线程需要从任务缓存模块中不断地取任务执行，帮助线程从阻塞队列中获取任务，实现线程管理模块和任务管理模块之间的通信。这部分策略由getTask方法实现，其执行流程如下图所示：<br><img src="/../images/dailyrecord/%E8%8E%B7%E5%8F%96%E4%BB%BB%E5%8A%A1%E6%B5%81%E7%A8%8B.png" alt="img"><br>getTask这部分进行了多次判断，为的是控制线程的数量，使其符合线程池的状态。如果线程池现在不应该持有那么多线程，则会返回null值。工作线程Worker会不断接收新任务去执行，而当工作线程Worker接收不到任务的时候，就会开始被回收。</p><h4 id="任务拒绝"><a href="#任务拒绝" class="headerlink" title="任务拒绝"></a>任务拒绝</h4><p>任务拒绝模块是线程池的保护部分，线程池有一个最大的容量，当线程池的任务缓存队列已满，并且线程池中的线程数目达到maximumPoolSize时，就需要拒绝掉该任务，采取任务拒绝策略，保护线程池。</p><p>拒绝策略是一个接口，其设计如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">RejectedExecutionHandler</span> &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">rejectedExecution</span><span class="hljs-params">(Runnable r, ThreadPoolExecutor executor)</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>用户可以通过实现这个接口去定制拒绝策略，也可以选择JDK提供的四种已有拒绝策略，其特点如下：<br><img src="/../images/dailyrecord/%E4%BB%BB%E5%8A%A1%E6%8B%92%E7%BB%9D%E7%AD%96%E7%95%A5.png" alt="img"></p><h3 id="Worker线程管理"><a href="#Worker线程管理" class="headerlink" title="Worker线程管理"></a>Worker线程管理</h3><h4 id="Worker线程"><a href="#Worker线程" class="headerlink" title="Worker线程"></a>Worker线程</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Worker</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractQueuedSynchronizer</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Runnable</span>&#123;<br>    <span class="hljs-keyword">final</span> Thread thread;<span class="hljs-comment">//Worker持有的线程</span><br>    Runnable firstTask;<span class="hljs-comment">//初始化的任务，可以为null</span><br>&#125;<br></code></pre></td></tr></table></figure><p>Worker这个工作线程，实现了Runnable接口，并持有一个线程thread，一个初始化的任务firstTask。thread是在调用构造方法时通过ThreadFactory来创建的线程，可以用来执行任务；firstTask用它来保存传入的第一个任务，这个任务可以有也可以为null。如果这个值是非空的，那么线程就会在启动初期立即执行这个任务，也就对应核心线程创建时的情况；如果这个值是null，那么就需要创建一个线程去执行任务列表（workQueue）中的任务，也就是非核心线程的创建。</p><p>Worker执行任务的模型如下图所示：<br><img src="/../images/dailyrecord/Worker%E6%89%A7%E8%A1%8C%E6%B5%81%E7%A8%8B.png" alt="img"><br>线程池需要管理线程的生命周期，需要在线程长时间不运行的时候进行回收。线程池使用一张Hash表去持有线程的引用，这样可以通过添加引用、移除引用这样的操作来控制线程的生命周期。这个时候重要的就是如何判断线程是否在运行。</p><p>Worker是通过继承AQS，使用AQS来实现独占锁这个功能。<strong>没有使用可重入锁ReentrantLock，而是使用AQS，为的就是实现不可重入的特性去反应线程现在的执行状态。</strong></p><p>1.lock方法一旦获取了独占锁，表示当前线程正在执行任务中。<br>2.如果正在执行任务，则不应该中断线程。<br>3.如果该线程现在不是独占锁的状态，也就是空闲的状态，说明它没有在处理任务，这时可以对该线程进行中断。<br>4.线程池在执行shutdown方法或tryTerminate方法时会调用interruptIdleWorkers方法来中断空闲的线程，interruptIdleWorkers方法会使用tryLock方法来判断线程池中的线程是否是空闲状态；如果线程是空闲状态则可以安全回收。</p><p>在线程回收过程中就使用到了这种特性，回收过程如下图所示：<br><img src="/../images/dailyrecord/%E7%BA%BF%E7%A8%8B%E5%9B%9E%E6%94%B6%E7%AD%96%E7%95%A5.png" alt="img"></p><h4 id="Worker线程增加"><a href="#Worker线程增加" class="headerlink" title="Worker线程增加"></a>Worker线程增加</h4><p>增加线程是通过线程池中的addWorker方法，该方法的功能就是增加一个线程，该方法不考虑线程池是在哪个阶段增加的该线程，这个分配线程的策略是在上个步骤完成的，该步骤仅仅完成增加线程，并使它运行，最后返回是否成功这个结果。addWorker方法有两个参数：firstTask、core。firstTask参数用于指定新增的线程执行的第一个任务，该参数可以为空；core参数为true表示在新增线程时会判断当前活动线程数是否少于corePoolSize，false表示新增线程前需要判断当前活动线程数是否少于maximumPoolSize，其执行流程如下图所示：</p><p><img src="/../images/dailyrecord/%E7%94%B3%E8%AF%B7%E7%BA%BF%E7%A8%8B%E6%B1%A0%E6%B5%81%E7%A8%8B.png" alt="img"></p><h4 id="Worker线程回收"><a href="#Worker线程回收" class="headerlink" title="Worker线程回收"></a>Worker线程回收</h4><p>线程池中线程的销毁依赖JVM自动的回收，线程池做的工作是根据当前线程池的状态维护一定数量的线程引用，防止这部分线程被JVM回收，当线程池决定哪些线程需要回收时，只需要将其引用消除即可。Worker被创建出来后，就会不断地进行轮询，然后获取任务去执行，核心线程可以无限等待获取任务，非核心线程要限时获取任务。当Worker无法获取到任务，也就是获取的任务为空时，循环会结束，Worker会主动消除自身在线程池内的引用。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>  <span class="hljs-keyword">while</span> (task != <span class="hljs-literal">null</span> || (task = getTask()) != <span class="hljs-literal">null</span>) &#123;<br>    <span class="hljs-comment">//执行任务</span><br>  &#125;<br>&#125; <span class="hljs-keyword">finally</span> &#123;<br>  processWorkerExit(w, completedAbruptly);<span class="hljs-comment">//获取不到任务时，主动回收自己</span><br>&#125;<br></code></pre></td></tr></table></figure><p>线程回收的工作是在processWorkerExit方法完成的：<br><img src="/../images/dailyrecord/%E7%BA%BF%E7%A8%8B%E9%94%80%E6%AF%81%E6%B5%81%E7%A8%8B.png" alt="img"><br>事实上，在这个方法中，将线程引用移出线程池就已经结束了线程销毁的部分。但由于引起线程销毁的可能性有很多，线程池还要判断是什么引发了这次销毁，是否要改变线程池的现阶段状态，是否要根据新状态，重新分配线程。</p><h4 id="Worker线程执行任务"><a href="#Worker线程执行任务" class="headerlink" title="Worker线程执行任务"></a>Worker线程执行任务</h4><p>在Worker类中的run方法调用了runWorker方法来执行任务，runWorker方法的执行过程如下：</p><ol><li>while循环不断地通过getTask()方法获取任务。 </li><li>getTask()方法从阻塞队列中取任务。 </li><li>如果线程池正在停止，那么要保证当前线程是中断状态，否则要保证当前线程不是中断状态。 </li><li>执行任务。 </li><li>如果getTask结果为null则跳出循环，执行processWorkerExit()方法，销毁线程。</li></ol><p>执行流程如下图所示：<br><img src="/../images/dailyrecord/%E6%89%A7%E8%A1%8C%E4%BB%BB%E5%8A%A1%E6%B5%81%E7%A8%8B.png" alt="img"></p><h2 id="线程池在业务中的实践"><a href="#线程池在业务中的实践" class="headerlink" title="线程池在业务中的实践"></a>线程池在业务中的实践</h2><h3 id="业务背景"><a href="#业务背景" class="headerlink" title="业务背景"></a>业务背景</h3><p>在当今的互联网业界，为了最大程度利用CPU的多核性能，并行运算的能力是不可或缺的。通过线程池管理线程获取并发性是一个非常基础的操作，让我们来看两个典型的使用线程池获取并发性的场景。</p><p>场景1：快速响应用户请求</p><p>描述：用户发起的实时请求，服务追求响应时间。比如说用户要查看一个商品的信息，那么我们需要将商品维度的一系列信息如商品的价格、优惠、库存、图片等等聚合起来，展示给用户。</p><p>分析：从用户体验角度看，这个结果响应的越快越好，如果一个页面半天都刷不出，用户可能就放弃查看这个商品了。而面向用户的功能聚合通常非常复杂，伴随着调用与调用之间的级联、多级级联等情况，业务开发同学往往会选择使用线程池这种简单的方式，将调用封装成任务并行的执行，缩短总体响应时间。另外，使用线程池也是有考量的，这种场景最重要的就是获取最大的响应速度去满足用户，所以应该不设置队列去缓冲并发任务，调高corePoolSize和maxPoolSize去尽可能创造多的线程快速执行任务。<br><img src="/../images/dailyrecord/%E5%B9%B6%E8%A1%8C%E6%89%A7%E8%A1%8C%E4%BB%BB%E5%8A%A1%E6%8F%90%E5%8D%87%E4%BB%BB%E5%8A%A1%E5%93%8D%E5%BA%94%E9%80%9F%E5%BA%A6.png" alt="img"></p><p>场景2：快速处理批量任务</p><p>描述：离线的大量计算任务，需要快速执行。比如说，统计某个报表，需要计算出全国各个门店中有哪些商品有某种属性，用于后续营销策略的分析，那么我们需要查询全国所有门店中的所有商品，并且记录具有某属性的商品，然后快速生成报表。</p><p>分析：这种场景需要执行大量的任务，我们也会希望任务执行的越快越好。这种情况下，也应该使用多线程策略，并行计算。但与响应速度优先的场景区别在于，这类场景任务量巨大，并不需要瞬时的完成，而是关注如何使用有限的资源，尽可能在单位时间内处理更多的任务，也就是吞吐量优先的问题。所以应该设置队列去缓冲并发任务，调整合适的corePoolSize去设置处理任务的线程数。在这里，设置的线程数过多可能还会引发线程上下文切换频繁的问题，也会降低处理任务的速度，降低吞吐量。<br><img src="/../images/dailyrecord/%E5%BF%AB%E9%80%9F%E5%A4%84%E7%90%86%E4%BB%BB%E5%8A%A1.png" alt="img"></p><h3 id="实际问题及方案思考"><a href="#实际问题及方案思考" class="headerlink" title="实际问题及方案思考"></a>实际问题及方案思考</h3><p>线程池使用面临的核心的问题在于：线程池的参数并不好配置。一方面线程池的运行机制不是很好理解，配置合理需要强依赖开发人员的个人经验和知识；另一方面，线程池执行的情况和任务类型相关性较大，IO密集型和CPU密集型的任务运行起来的情况差异非常大，这导致业界并没有一些成熟的经验策略帮助开发人员参考。</p><p>关于线程池配置不合理引发的故障，公司内部有较多记录，下面举一些例子：</p><p>Case1：2018年XX页面展示接口大量调用降级：</p><p>事故描述：XX页面展示接口产生大量调用降级，数量级在几十到上百。</p><p>事故原因：该服务展示接口内部逻辑使用线程池做并行计算，由于没有预估好调用的流量，导致最大核心数设置偏小，大量抛出RejectedExecutionException，触发接口降级条件，示意图如下：<br><img src="/../images/dailyrecord/%E7%BA%BF%E7%A8%8B%E6%95%B0%E6%A0%B8%E5%BF%83%E8%BF%87%E5%B0%8F.png" alt="img"></p><p>Case2：2018年XX业务服务不可用S2级故障</p><p>事故描述：XX业务提供的服务执行时间过长，作为上游服务整体超时，大量下游服务调用失败。</p><p>事故原因：该服务处理请求内部逻辑使用线程池做资源隔离，由于队列设置过长，最大线程数设置失效，导致请求数量增加时，大量任务堆积在队列中，任务执行时间过长，最终导致下游服务的大量调用超时失败。示意图如下：<br><img src="/../images/dailyrecord/%E9%98%9F%E5%88%97%E8%AE%BE%E7%BD%AE%E8%BF%87%E9%95%BF.png" alt="img"></p><p>业务中要使用线程池，而使用不当又会导致故障，那么我们怎样才能更好地使用线程池呢？针对这个问题，我们下面延展几个方向：</p><ol><li>能否不用线程池?</li></ol><p>回到最初的问题，业务使用线程池是为了获取并发性，对于获取并发性，是否可以有什么其他的方案呢替代？我们尝试进行了一些其他方案的调研：</p><p><img src="/../images/dailyrecord/%E5%B9%B6%E5%8F%91%E6%80%A7%E8%B0%83%E7%A0%94.png" alt="img"></p><p>综合考虑，这些新的方案都能在某种情况下提升并行任务的性能，然而本次重点解决的问题是如何更简易、更安全地获得的并发性。另外，Actor模型的应用实际上甚少，只在Scala中使用广泛，协程框架在Java中维护的也不成熟。这三者现阶段都不是足够的易用，也并不能解决业务上现阶段的问题。</p><ol start="2"><li>追求参数设置合理性？</li></ol><p>有没有一种计算公式，能够让开发同学很简易地计算出某种场景中的线程池应该是什么参数呢？</p><p>带着这样的疑问，我们调研了业界的一些线程池参数配置方案：</p><p><img src="/../images/dailyrecord/%E7%BA%BF%E7%A8%8B%E6%B1%A0%E5%8F%82%E6%95%B0%E9%85%8D%E7%BD%AE.png" alt="img"><br>调研了以上业界方案后，我们并没有得出通用的线程池计算方式。并发任务的执行情况和任务类型相关，IO密集型和CPU密集型的任务运行起来的情况差异非常大，但这种占比是较难合理预估的，这导致很难有一个简单有效的通用公式帮我们直接计算出结果。</p><ol start="3"><li>线程池参数动态化？</li></ol><p>尽管经过谨慎的评估，仍然不能够保证一次计算出来合适的参数，那么我们是否可以将修改线程池参数的成本降下来，这样至少可以发生故障的时候可以快速调整从而缩短故障恢复的时间呢？基于这个思考，我们是否可以将线程池的参数从代码中迁移到分布式配置中心上，实现线程池参数可动态配置和即时生效，线程池参数动态化前后的参数修改流程对比如下：</p><p><img src="/../images/dailyrecord/%E5%8A%A8%E6%80%81%E7%BA%BF%E7%A8%8B%E6%B1%A0%E9%85%8D%E7%BD%AE%E5%AF%B9%E6%AF%94.png" alt="img"></p><p>基于以上三个方向对比，我们可以看出参数动态化方向简单有效。</p>]]></content>
    
    
    <categories>
      
      <category>日常笔记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Java并发编程</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Java 内存模型和原子性、可见性、有序性</title>
    <link href="/%5Bobject%20Object%5D/2023/07/Java%E5%B9%B6%E5%8F%91-Java-%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B%E5%92%8C%E5%8E%9F%E5%AD%90%E6%80%A7%E3%80%81%E5%8F%AF%E8%A7%81%E6%80%A7%E3%80%81%E6%9C%89%E5%BA%8F%E6%80%A7/"/>
    <url>/%5Bobject%20Object%5D/2023/07/Java%E5%B9%B6%E5%8F%91-Java-%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B%E5%92%8C%E5%8E%9F%E5%AD%90%E6%80%A7%E3%80%81%E5%8F%AF%E8%A7%81%E6%80%A7%E3%80%81%E6%9C%89%E5%BA%8F%E6%80%A7/</url>
    
    <content type="html"><![CDATA[<h1 id="Java-内存模型和原子性、可见性、有序性"><a href="#Java-内存模型和原子性、可见性、有序性" class="headerlink" title="Java 内存模型和原子性、可见性、有序性"></a>Java 内存模型和原子性、可见性、有序性</h1><h2 id="并发编程三个核心问题"><a href="#并发编程三个核心问题" class="headerlink" title="并发编程三个核心问题"></a>并发编程三个核心问题</h2><ul><li>分工：如何高效地拆解任务并分配给线程</li><li>同步：线程之间如何协作</li><li>互斥：保证同一时刻只允许一个线程访问共享资源</li></ul><h2 id="原子性、可见性和有序性"><a href="#原子性、可见性和有序性" class="headerlink" title="原子性、可见性和有序性"></a>原子性、可见性和有序性</h2><h3 id="原子性"><a href="#原子性" class="headerlink" title="原子性"></a>原子性</h3><p>指在一个操作中就是cpu不可以在中途暂停然后再调度，既不被中断操作，要不执行完成，要不就不执行。</p><p>操作系统中会产生任务切换情况，即只允许一个进程执行一小段时间（时间片），然后让该进程让出CPU，切换成另外的进程执行任务，图片如下：<br><img src="/../images/dailyrecord/%E6%97%B6%E9%97%B4%E7%89%87%E8%B0%83%E5%BA%A6.webp" alt="img"></p><p>但是，高级代码的语句是由多条CPU指令组成的，例如count +&#x3D; 1至少包含了三个步骤：</p><ul><li>指令1：首先，需要把变量count从内存加载到CPU的寄存器；</li><li>指令2：之后，在寄存器中执行+1操作；</li><li>指令3：最后，将结果写入内存（缓存机制导致可能写入的是CPU缓存而不是内存）</li></ul><p>这种情况就导致两个线程都执行count +&#x3D; 1这句代码时，会因为任务调度而导致这句话的CPU指令被拆开，使得最终结果不是我们预期的</p><p><img src="/../images/dailyrecord/%E9%9D%9E%E5%8E%9F%E5%AD%90%E6%80%A7%E6%89%A7%E8%A1%8C%E8%B7%AF%E5%BE%84.webp" alt="img"></p><p>上述执行流程中，最终的count结果为1，而不是预期的2。并发编程的原子性就是保证一个或者多个CPU操作不会被操作系统中断，从而保证最终结果的正确。</p><p>JMM通过synchronized和Lock实现原子性，因为 synchronized 和 Lock 能够保证任一时刻只有一个线程访问该代码块。</p><h3 id="可见性"><a href="#可见性" class="headerlink" title="可见性"></a>可见性</h3><p>指当多个线程访问同一个变量时，一个线程修改了这个变量的值，其他线程能够立即看得到修改的值。</p><p>在单核CPU中，不会出现可见性线程，因为所有线程都是在同一处CPU缓存中修改变量，这保证了CPU缓存和内存的一致性<br><img src="/../images/dailyrecord/%E5%8D%95%E6%A0%B8CPU%E7%BC%93%E5%AD%98.webp" alt="img"></p><p>在多核CPU架构中，线程之间的共享变量存在主内存中，每个线程都有一个私有的本地内存，存储了该线程以读、写共享变量的副本。本地内存是Java内存模型的一个抽象概念，并不真实存在。它涵盖了缓存、写缓冲区、寄存器等。<br><img src="/../images/dailyrecord/%E5%A4%9A%E6%A0%B8CPU%E7%BC%93%E5%AD%98.webp" alt="img"></p><p>Java线程之间的通信由Java内存模型（简称JMM）控制，从抽象的角度来说，JMM定义了线程和主内存之间的抽象关系。JMM的抽象示意图如图所示：<br><img src="/../images/dailyrecord/JMM%E6%8A%BD%E8%B1%A1%E7%BB%93%E6%9E%84.jpg" alt="img"><br>从图中可以看出：</p><ol><li>所有的共享变量都存在主内存中。</li><li>每个线程都保存了一份该线程使用到的共享变量的副本。</li><li>如果线程A与线程B之间要通信的话，必须经历下面2个步骤：<ol><li>线程A将本地内存A中更新过的共享变量刷新到主内存中去。</li><li>线程B到主内存中去读取线程A之前已经更新过的共享变量。</li></ol></li></ol><p>因此，由于线程A无法访问线程B的工作区域，导致线程间的通信必须通过主存储。但是根据JMM的规定，线程对共享变量的所有操作必须在自己的本地内存中进行，无法直接从主存储中读取。这就导致即使线程A更改了主存储区域的变量，线程B也会因为JMM的特性无法察觉到线程A的更新，这样线程B依旧使用旧的数据，这就是可见性问题。</p><p>例如以下程序，两个线程同时对公共变量count进行更改，但是各自线程对编程的改动位置都是各自CPU缓存中，CPU缓存变量同步到统一内存中需要部分时间，这就导致count的最终结果处于10000到20000之间（具体数值取决于同步的速度）。</p><figure class="highlight axapta"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs axapta"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span> &#123;<br>  <span class="hljs-keyword">private</span> <span class="hljs-built_in">long</span> <span class="hljs-keyword">count</span> = <span class="hljs-number">0</span>;<br>  <span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> add10K() &#123;<br>    <span class="hljs-built_in">int</span> idx = <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">while</span>(idx++ &lt; <span class="hljs-number">10000</span>) &#123;<br>      <span class="hljs-keyword">count</span> += <span class="hljs-number">1</span>;<br>    &#125;<br>  &#125;<br>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-built_in">long</span> calc() &#123;<br>    <span class="hljs-keyword">final</span> Test test = <span class="hljs-keyword">new</span> Test();<br>    <span class="hljs-comment">// 创建两个线程，执行add()操作</span><br>    Thread th1 = <span class="hljs-keyword">new</span> Thread(()-&gt;&#123;<br>      test.add10K();<br>    &#125;);<br>    Thread th2 = <span class="hljs-keyword">new</span> Thread(()-&gt;&#123;<br>      test.add10K();<br>    &#125;);<br>    <span class="hljs-comment">// 启动两个线程</span><br>    th1.start();<br>    th2.start();<br>    <span class="hljs-comment">// 等待两个线程执行结束</span><br>    th1.<span class="hljs-keyword">join</span>();<br>    th2.<span class="hljs-keyword">join</span>();<br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">count</span>;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>volatile 关键字可以保证可见性。当一个共享变量被 volatile 关键字修饰时，它会保证修改的值立即被其他的线程看到，即<strong>修改的值立即更新到主存中</strong>，当其他线程需要读取时，它会去内存中读取新值。synchronized 和 Lock 也可以保证可见性，因为它们可以保证任一时刻只有一个线程能访问共享资源，并在其释放锁之前将修改的变量刷新到内存中。</p><h3 id="有序性"><a href="#有序性" class="headerlink" title="有序性"></a>有序性</h3><p>指的是程序执行的顺序按照代码的先后顺序执行，JVM的编译优化会打乱程序的执行顺序</p><h4 id="重排序"><a href="#重排序" class="headerlink" title="重排序"></a>重排序</h4><p>简单地说，每一个指令都会包含多个步骤，每个步骤可能使用不同的硬件。因此，流水线技术产生了，它的原理是指令1还没有执行完，就可以开始执行指令2，而不用等到指令1执行结束之后再执行指令2，这样就大大提高了效率。</p><p>但是，流水线技术最害怕中断，恢复中断的代价是比较大的，所以我们要想尽办法不让流水线中断。指令重排就是减少中断的一种技术。</p><p>我们分析一下下面这个代码的执行情况：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">a = b + <span class="hljs-type">c</span><br><span class="hljs-variable">d</span> <span class="hljs-operator">=</span> e - f<br></code></pre></td></tr></table></figure><p>先加载b、c（注意，即有可能先加载b，也有可能先加载c），但是在执行add(b,c)的时候，需要等待b、c装载结束才能继续执行，也就是增加了停顿，那么后面的指令也会依次有停顿，这降低了计算机的执行效率。</p><p>为了减少这个停顿，我们可以先加载e和f,然后再去加载add(b,c),这样做对程序（串行）是没有影响的，但却减少了停顿。既然add(b,c)需要停顿，那还不如去做一些有意义的事情。</p><p>综上所述，指令重排对于提高CPU处理性能十分必要。虽然由此带来了乱序的问题，但是这点牺牲是值得的。</p><p>指令重排一般分为以下三种：</p><ul><li>编译器优化重排：<br>编译器在不改变单线程程序语义的前提下，可以重新安排语句的执行顺序。</li><li>指令并行重排：<br>现代处理器采用了指令级并行技术来将多条指令重叠执行。如果不存在数据依赖性(即后一个执行的语句无需依赖前面执行的语句的结果)，处理器可以改变语句对应的机器指令的执行顺序。</li><li>内存系统重排：<br>由于处理器使用缓存和读写缓存冲区，这使得加载(load)和存储(store)操作看上去可能是在乱序执行，因为三级缓存的存在，导致内存与缓存的数据同步存在时间差。</li></ul><p><img src="/../images/dailyrecord/%E6%8C%87%E4%BB%A4%E9%87%8D%E6%8E%92%E5%BA%8F.webp" alt="img"><br>指令重排可以保证串行语义一致，但是没有义务保证多线程间的语义也一致。<strong>所以在多线程下，指令重排序可能会导致一些问题。</strong></p><p>在Java领域一个经典的案例就是利用双重检查创建单例对象，例如下面的代码：在获取实例<code>getInstance()</code>的方法中，我们首先判断instance是否为空，如果为空，则锁定<code>Singleton.class</code>并再次检查instance是否为空，如果还为空则创建<code>Singleton</code>的一个实例。</p><figure class="highlight smali"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs smali">public class Singleton &#123;<br> <span class="hljs-keyword"> static</span> Singleton instance;<br> <span class="hljs-keyword"> static</span> Singleton getInstance()&#123;<br>   <span class="hljs-built_in"> if </span>(instance == null) &#123;<br>      synchronized(Singleton.class) &#123;<br>       <span class="hljs-built_in"> if </span>(instance == null)<br>         <span class="hljs-built_in"> instance </span>=<span class="hljs-built_in"> new </span>Singleton();<br>        &#125;<br>    &#125;<br>   <span class="hljs-built_in"> return </span>instance;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这看上去一切都很完美，无懈可击，但实际上这个getInstance()方法并不完美。问题出在哪里呢？出在new操作上，我们以为的new操作应该是：</p><ul><li>分配一块内存M</li><li>在内存M上初始化Singleton对象</li><li>然后M的地址赋值给instance变量</li></ul><p>但是实际上优化后的执行路径却是这样的：</p><ul><li>分配一块内存M</li><li>将M的地址赋值给instance变量</li><li>最后在内存M上初始化Singleton对象</li></ul><p>优化后会导致什么问题呢？我们假设线程A先执行getInstance()方法，当执行完 new Singleton() 的第二个时恰好发生了线程切换（即完成了内存分配，但是没有初始化），切换到了线程B上。如果此时线程B也执行getInstance()方法，那么线程B在执行第一个判断时会发现 instance !&#x3D; null ，所以直接返回instance，而此时的instance是没有初始化过的，如果我们这个时候访问 instance 的成员变量就可能触发空指针异常。</p><p>所以<strong>双重校验锁下的单例模式中，需要对变量添加volatile关键字</strong>，即<code>static volatile Singleton instance</code></p><p>在Java里面，可以通过 volatile 关键字来保证一定的“有序性”。另外可以通过 synchronized 和 Lock 来保证有序性，很显然，synchronized 和 Lock 保证每个时刻是只有一个线程执行同步代码，相当于是让线程顺序执行同步代码，自然就保证来有序性。</p><h5 id="as-if-serial-语义"><a href="#as-if-serial-语义" class="headerlink" title="as-if-serial 语义"></a>as-if-serial 语义</h5><p>as-if-serial 语义的意思是：不管怎么重排序（编译器和处理器为了提高并行度），（单线程）程序的执行结果不能被改变。编译器、runtime和处理器都必须遵守 as-if-serial 语义。<br>为了遵守 as-if-serial 语义，编译器和处理器不会对存在数据依赖关系的操作做重排序，因为这种重排序会改变执行结果。但是，如果操作之间不存在数据依赖关系，这些操作就可能被编译器和处理器重排序。</p><h5 id="happens-before-原则"><a href="#happens-before-原则" class="headerlink" title="happens-before 原则"></a>happens-before 原则</h5><p>只靠 synchronized 和 volatile 关键字来保证原子性、可见性以及有序性，那么编写并发程序可能会显得十分麻烦，幸运的是，从JDK 5 开始，Java 使用新的 JSR-133 内存模型，提供了 happens-before 原则 来辅助保证程序执行的原子性、可见性和有序性的问题，它是判断数据十分存在竞争、线程十分安全的一句。happens-before 原则内容如下：</p><ol><li>程序顺序原则：即在一个线程内必须保证语义串行，也就是说按照代码顺序执行。</li><li>锁规则：解锁（unlock）操作必然发生在后续的同一个锁的加锁（lock）之前，也就是说，如果对于一个锁解锁后，再加锁，那么加锁的动作必须在解锁动作之后（同一个锁）。</li><li>volatile规则：volatile变量的写，先发生于读，这保证了volatile变量的可见性，简单理解就是，volatile变量在每次被线程访问时，都强迫从主内存中读该变量的值，而当该变量发生变化时，又会强迫将最新的值刷新到主内存，任何时刻，不同的线程总是能够看到该变量的最新值。</li><li>线程启动规则：线程的 start() 方法先于它的每一个动作，即如果线程A在执行线程B的 start 方法之前修改了共享变量的值，那么当线程B执行start方法时，线程A对共享变量的修改对线程B可见。</li><li>传递性：A先于B，B先于C，那么A必然先于C。</li><li>线程终止原则：线程的所有操作先于线程的终结，<code>Thread.join()</code>方法的作用是等待当前执行的线程终止。假设线程B终止之前，修改了共享变量，线程A从线程B的join方法成功返回，线程B对共享变量的修改将对线程A可见。</li><li>线程中断规则：对线程<code>interrupt()</code>方法的调用先行发生于被中断线程的代码检查到中断事件的发生，可以通过 <code>Thread.interrupted()</code>方法检测线程十分中断。</li><li>对象终结规则：对象的构造函数执行，结束先于<code>finalize()</code>方法。</li></ol><h2 id="Java内存模型（JMM）"><a href="#Java内存模型（JMM）" class="headerlink" title="Java内存模型（JMM）"></a>Java内存模型（JMM）</h2><p>并发编程模型有两个关键问题：</p><ul><li>线程间通信问题，即线程之间以何种机制来交换信息</li><li>线程间如何同步，即线程以何种机制来控制不同线程间操作发生的相对顺序<br>有两种并发模型可以解决这两个问题：</li><li>消息传递并发模型</li><li>共享内存并发模型</li></ul><p>这两种模型之间的区别如下表所示：<br><img src="/../images/dailyrecord/%E4%B8%A4%E7%A7%8D%E6%A8%A1%E5%9E%8B%E5%A4%84%E7%90%86%E5%B9%B6%E5%8F%91.png" alt="img"><br>Java使用的是共享内存并发模型</p><p><img src="/../images/dailyrecord/java%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B%E6%8A%BD%E8%B1%A1%E7%BB%93%E6%9E%84.png" alt="img"></p><p>上图是Java的内存结构。对于每一个Java线程来说，栈是线程私有的，但是堆是共有的。这就意味着栈中的变量（局部变量、方法定义参数、异常处理器参数）不会在线程之间共享，也就不会有内存可见性（下文会说到）的问题，也不受内存模型的影响。而在堆中的变量是共享的，本文称为共享变量。</p><h2 id="volatile"><a href="#volatile" class="headerlink" title="volatile"></a>volatile</h2><p>volatile 是Java虚拟机提供的轻量级的同步机制。volatile 关键字有如下两个作用：</p><ol><li>保证被 volatile 修饰的共享变量对所有线程总是可见的，也就是当一个线程修改了被 volatile 修饰共享变量的值，新值总是可以被其他线程立即得知</li><li>指令重排序优化</li></ol><h3 id="volatile的可见性"><a href="#volatile的可见性" class="headerlink" title="volatile的可见性"></a>volatile的可见性</h3><p>关于 volatile 的可见性作用，我们必须意思到被 volatile 修饰的变量对所有线程总是立即可见的，对于 volatile 变量的所有写操作总是能立刻反应到其他线程中。</p><p>在一个场景中，两个线程都需要对data进行操作，如果没有volatile，那么两个线程都会将变量的副本加载到自己的工作内存里，在线程代码运行的过程中，就可以直接从本地缓存里加载变量副本，不需要从主内存加载变量值。这种方式会导致虽然线程A和B操作的都是data这个变量，但是由于每次读区的都是本地线程的副本，导致两个线程都没有办法读取到其他线程对变量的改动，使得可见性出现问题。流程如下图所示：<br><img src="/../images/dailyrecord/%E6%97%A0volatile.webp" alt="img"></p><p>但是有了volatile之后，线程对变量的操作逻辑就有了些许改变<br><img src="/../images/dailyrecord/%E6%9C%89volatile.webp" alt="img"></p><p>volatile写入的时候，会将本线程中的变量都强制刷新，还会将该变量在其他线程中的缓存设置无效，这样保证了其他线程能够第一时间读取到volatile变量的更改。（volatile变量并不是不允许线程缓存，而是采用强制刷新的模式）</p><h3 id="volatile的有序性"><a href="#volatile的有序性" class="headerlink" title="volatile的有序性"></a>volatile的有序性</h3><p>volatile 关键字另一个作用就是禁止指令重排优化，从而避免多线程环境下程序出现乱序执行的现象，关于指令重排优化前面已经分析过，这里主要简单说明一下 volatile 是如何实现禁止指令重排优化的。先了解一个概念，内存屏障（Memory Barrier）</p><h4 id="JVM的内存屏障"><a href="#JVM的内存屏障" class="headerlink" title="JVM的内存屏障"></a>JVM的内存屏障</h4><p>不同硬件实现内存屏障的方式不同，Java 内存模型屏蔽了这些底层硬件平台的差异，由 JVM 来为不同平台生产相应的机器码。JVM中提供了四类内存屏障指令：<br><img src="/../images/dailyrecord/%E5%86%85%E5%AD%98%E5%B1%8F%E9%9A%9C.webp" alt="img"><br>内存屏障，又称内存栅栏，是一个CPU指令，它的作用有两个：</p><p>一是保证特定操作的执行顺序<br>二是保证某些变量的内存可见性（利用该特性实现 volatile 的内存可见性）</p><p>由于编译器和处理器都能执行指令重排优化。如果在指令间插入一条 Memory Barrier 则会高速编译器和CPU，不管什么指令都不能和这条 Memory Barrier 指令重排序，也就是说通过插入内存屏障禁止在内存屏障前后的指令执行重排序优化。</p><p>Memory Barrier 的另外一个作用是强制刷出各种 CPU 的缓存数据，因此任何 CPU 上的线程都能读取到这些数据的最新版本。</p><p>前面提到过重排序分为编译器重排序和处理器重排序。为来实现 volatile 内存语义，JMM 会分别限制这两种类型的重排序类型。下面是JMM针对编译器制定的 volatile 重排序规则表（这里的普通读写表示读+写，例如a&#x3D;b+1，先读取b的值，再进行写入）：<br><img src="/../images/dailyrecord/volatile%E9%87%8D%E6%8E%92%E5%BA%8F%E8%A1%A8.webp" alt="img"></p><ul><li>第一个操作是volatile读时，不管第二个操作是什么，都不能重排序。这个规则确保 volatile读之后的操作不会被编译器重排序到volatile读之前。确保volatile读操作读取内存里的最新值是程序员希望读到的、操作的值</li><li>第二个操作是volatile写时，不管第一个操作是什么，都不能重排序。这个规则确保 volatile写之前的操作不会被编译器重排序到volatile写之后。换句话说，以volatile写这行代码为分割线，之前的对共享变量的各种读写操作的指令不管如何进行重排序，都不可能跑到volatile写操作之后执行，确保volatile写操作刷新内存里共享变量的值时程序员希望发生的变动都能够正确的刷新到内存中</li></ul><p>例子：</p><ol><li><p>普通写与volatile写</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">volatile</span> <span class="hljs-type">int</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br><span class="hljs-type">int</span> <span class="hljs-variable">b</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">A</span> <span class="hljs-params">()</span>&#123;<br>    b = <span class="hljs-number">2</span>;    <span class="hljs-comment">// 1 普通写</span><br>    a = <span class="hljs-number">1</span>;      <span class="hljs-comment">// 2 volatile写</span><br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">B</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">c</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">if</span> (a == <span class="hljs-number">1</span>)    <span class="hljs-comment">// 3  volatile读</span><br>        c = b;      <span class="hljs-comment">// 4 普通写</span><br>&#125;  <br></code></pre></td></tr></table></figure><p>1、2发生重排，先执行2，再执行方法B，最后执行1，会导致c的结果是1而不是预想中的2</p></li><li><p>volatile读与普通写</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">volatile</span> <span class="hljs-type">int</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;<br><span class="hljs-type">int</span> <span class="hljs-variable">b</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">A</span> <span class="hljs-params">()</span>&#123;<br>    a = <span class="hljs-number">2</span>; <span class="hljs-comment">//1 volatile写</span><br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">B</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">c</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">if</span> (a == <span class="hljs-number">1</span>)    <span class="hljs-comment">// 3  volatile读</span><br>        c = b;      <span class="hljs-comment">// 4 普通写</span><br>&#125;  <br></code></pre></td></tr></table></figure><p>3、4发生重排，先执行4，再执行方法A，最后执行3，这会导致方法B逻辑判断不成立，但是c却等于1</p></li></ol><p>为了实现 volatile 的内存语义，编译在生成字节码时，会在指令序列中插入内存屏障来禁止特定类型的处理器重排序。对于编译器来说，发现一个最优布置来最小化插入屏障的总数几乎不可能。为此，JMM 采取保守策略。下面是基于保守策略的JMM内存屏障插入策略。</p><ul><li>在每个volatile写操作的前面插入一个StoreStore屏障</li><li>在每个volatile写操作的后面插入一个StoreLoad屏障</li><li>在每个volatile读操作的后面插入一个LoadLoad屏障</li><li>在每个volatile读操作的后面插入一个LoadStore屏障</li></ul><p><img src="/../images/dailyrecord/volatile%E5%86%99%E7%9A%84%E5%B1%8F%E9%9A%9C.webp" alt="img"></p><p><img src="/../images/dailyrecord/volatile%E8%AF%BB%E7%9A%84%E5%B1%8F%E9%9A%9C.webp" alt="img"></p>]]></content>
    
    
    <categories>
      
      <category>日常笔记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Java并发编程</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>线程池执行流程和队列选择</title>
    <link href="/%5Bobject%20Object%5D/2023/07/Java%E5%B9%B6%E5%8F%91-%E7%BA%BF%E7%A8%8B%E6%B1%A0%E6%89%A7%E8%A1%8C%E6%B5%81%E7%A8%8B%E5%92%8C%E9%98%9F%E5%88%97%E9%80%89%E6%8B%A9/"/>
    <url>/%5Bobject%20Object%5D/2023/07/Java%E5%B9%B6%E5%8F%91-%E7%BA%BF%E7%A8%8B%E6%B1%A0%E6%89%A7%E8%A1%8C%E6%B5%81%E7%A8%8B%E5%92%8C%E9%98%9F%E5%88%97%E9%80%89%E6%8B%A9/</url>
    
    <content type="html"><![CDATA[<h1 id="线程池执行流程和队列选择"><a href="#线程池执行流程和队列选择" class="headerlink" title="线程池执行流程和队列选择"></a>线程池执行流程和队列选择</h1><h2 id="线程池ThreadPoolExecutor的参数"><a href="#线程池ThreadPoolExecutor的参数" class="headerlink" title="线程池ThreadPoolExecutor的参数"></a>线程池ThreadPoolExecutor的参数</h2><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs arduino"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ThreadPoolExecutor</span><span class="hljs-params">(<span class="hljs-type">int</span> corePoolSize,</span></span><br><span class="hljs-params"><span class="hljs-function">                          <span class="hljs-type">int</span> maximumPoolSize,</span></span><br><span class="hljs-params"><span class="hljs-function">                          <span class="hljs-type">long</span> keepAliveTime,</span></span><br><span class="hljs-params"><span class="hljs-function">                          TimeUnit unit,</span></span><br><span class="hljs-params"><span class="hljs-function">                          BlockingQueue&lt;Runnable&gt; workQueue)</span></span>; <br></code></pre></td></tr></table></figure><ul><li>corePoolSize<br>核心线程大小。在创建了线程池后，默认情况下，线程池中并没有任何线程，而是等待有任务到来才创建线程去执行任务，除非调用了 prestartAllCoreThreads() 或者 prestartCoreThread() 方法，这两个方法是用来预创建线程，即在没有任务到来之前就创建 corePoolSize 个线程或者一个线程。<strong>默认情况下，在创建了线程池后，线程池中的线程数为0，当有任务来之后，就会创建一个线程去执行任务，当线程池中的线程数目达到 corePoolSize 后，就会把到达的任务放到缓存队列当中。</strong></li><li>maximumPoolSize<br>线程池最大线程数，表示线程池最多能创建多少线程</li><li>keepAliveTime<br>表示线程没有任务执行时最多保持多久时间会终止。默认情况下，只有当线程池中的线程数大于 corePoolSize 时，keepAliveTime 才会起作用，直到线程池中的线程数不大于 corePoolSize，即当线程池中的线程数大于 corePoolSize 时，如果一个线程空闲的时间达到 keepAliveTime，则会终止，直到线程池中的线程数不超过 corePoolSize。</li><li>unit<br>参数 keepAliveTime 的时间单位</li><li>workQueue<br>一个阻塞队列，用来存储等待执行的任务。一般阻塞队列有以下几种选择：ArrayBlockingQueue、LinkedBlockingQueue、SynchronousQueue和PriorityBlockingQueue。其余的还有DelayQueue、LinkedTransferQueue和LinkedBlockingDeque。<ul><li>ArrayBlockingQueue(int i)：规定大小的BlockingQueue，其构造必须指定大小，根据FIFO原则进行排序</li><li>LinkedBlockingQueue()或者(int i)：大小不固定的BlockingQueue。如果指定大小，生成的BlockingQueue有大小限制；如果不指定大小，其大小由Integer.MAX_VALUE决定。顺序为FIFO。</li><li>PriorityBlockingQueue()或者(int i)：类似于LinkedBlockingQueue，但是其所含对象的排序不是FIFO，而是根据对象的自然顺序或者其构造函数的Comparator决定</li><li>SynchronousQueue()：特殊的BlockingQueue，不存储元素的阻塞队列，每一个put操作必须等待take操作，否则不能添加元素。支持公平锁和非公平锁。Executors.newCachedThreadPool()就使用了SynchronousQueue，这个线程池根据需要创建新的线程，如果有空闲的线程就会重复使用。</li><li>DelayQueue：一个实现延迟获取的无界队列，在创建元素时，可以指定多久才能从队列中获取当前元素，只有期满之后才能从队列中获取元素。</li><li>LinkedTransferQueue：一个由链表结构组成的无界阻塞队列。LinkedTransferQueue可以算是 LinkedBolckingQueue 和 SynchronousQueue 的合体。SynchronousQueue 内部无法存储元素，当要添加元素的时候，需要阻塞。LinkedBolckingQueue 则内部使用了大量的锁，性能有所下降。相对于其他阻塞队列，LinkedTransferQueue多了 tryTransfer(E e)和transfer(E e) 方法。</li><li>LinkedBlockingDeque：一个由链表结构组成的双向阻塞队列。队列头部和尾部都可以添加和移除元素，多线程并发时，可以将锁的竞争降到最多一半。</li></ul></li><li>threadFactory<br>线程工厂，用于创建线程</li><li>handler<br>表示拒绝处理任务时的策略，有以下几种取值：<ul><li>ThreadPoolExecutor.AbortPolicy：丢弃任务并抛出 RejectedExecutionException 异常</li><li>ThreadPoolExecutor.DiscardPolicy：也是丢弃任务，但是不抛出异常</li><li>ThreadPoolExecutor.DiscardOldestPolicy：丢弃队列最前面的任务，然后重新尝试执行任务（重复此过程）</li><li>ThreadPoolExecutor.CallerRunsPolicy：由调用线程处理该任务，即这个任务并不是由线程池中的线程来执行，而是由主线程来执行</li></ul></li></ul><h2 id="任务创建、获取并执行的流程"><a href="#任务创建、获取并执行的流程" class="headerlink" title="任务创建、获取并执行的流程"></a>任务创建、获取并执行的流程</h2><p><img src="/../images/dailyrecord/%E7%BA%BF%E7%A8%8B%E6%B1%A0%E6%89%A7%E8%A1%8C%E6%B5%81%E7%A8%8B%E5%9B%BE.png" alt="img"><br>主要分为以下几个步骤：</p><ol><li>如果线程数小于coreSize，则创建核心线程addWorker(command, true)</li><li>如果coreSize已满，则任务进入队列中。</li><li>入队成功，则在队列中等待；入队失败，则尝试创建非核心线程执行command，addWorker(command, false)</li><li>第3步的addWorker创建取决于当前线程数是否大于等于maximumPoolSize。如果超过，创建失败，拒绝执行任务；如果成功，则创建线程执行</li></ol><p><strong>ThreadPoolExecutor#execute(Runnable command)源码：</strong><br><img src="/../images/dailyrecord/execute%E6%BA%90%E7%A0%81.png" alt="img"></p><p><strong>ThreadPoolExecutor#addWorker源码：</strong><br><img src="/../images/dailyrecord/addWorker%E6%BA%90%E7%A0%81.png" alt="img"></p><h2 id="不同的队列配置对业务的影响"><a href="#不同的队列配置对业务的影响" class="headerlink" title="不同的队列配置对业务的影响"></a>不同的队列配置对业务的影响</h2><ul><li>对于业务来说，最重要的是及时处理用户的请求，所以线程池中的队列大多选择不进行配置</li><li>如果要对线程池进行配置，那么尽可能使用SynchronousQueue队列。SynchronousQueue队列中，新任务加入时会被阻塞，直到有线程领取任务后才会结束阻塞状态，每次领取任务的数量取决于空闲线程的数量，即空闲线程有多少，就可以接受多少任务；而LinkedBlockingQueue和ArrayBlockingQueue队列每次<strong>只能允许一个任务加入</strong></li><li>如果使用LinkedBlockingQueue和ArrayBlockingQueue队列，队列的数量不能设置太小，否则就会出现大量线程空闲但是新任务被直接拒绝的情况。<ul><li>原因如下：接收任务的流量很大且处理任务很快，这时候队列接收和领取任务的频率变得很快，但是队列由于采用CAS机制来接收和领取任务，速率很慢。一旦队列设置过小且线程来不及处理原来的任务，很容易让队列容量达到上限。这个时候，根据流程图所示，外部任务会尝试创建新线程来解决任务。而如果线程以达到最大数量时，则会将新任务直接丢弃。</li><li>这样的话，最后的结果就是由于队列容量很小，线程池只有部分线程可以领取到任务，大多数线程空闲；而新加入的任务由于队列已满以及达到最大线程数而被直接丢弃。本质上来说，这个问题就是业务方面要求线程池内的队列线程流通量足够，但是LinkedBlockingQueue和ArrayBlockingQueue无法满足要求。</li></ul></li><li>SynchronousQueue队列则不会出现这个问题，当前时刻有多少空闲的消费者，则线程池就可以接收多少个任务，这可以保证足够的线程池流通性</li></ul>]]></content>
    
    
    <categories>
      
      <category>日常笔记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Java并发编程</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
